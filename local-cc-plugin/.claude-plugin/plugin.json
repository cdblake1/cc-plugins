{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "local-cc-plugin",
  "displayName": "Local CC Plugin",
  "version": "0.8.0",
  "description": "Umbrella plugin for Claude Code: the session-journal MCP server (cross-session memory), journaling + Bash guardrail hooks, compaction-safe handoffs, and view-setup commands.",
  "author": {
    "name": "Caleb Blake"
  },
  "license": "MIT",
  "keywords": ["journal", "memory", "guardrails", "context", "sqlite", "mcp"],
  "userConfig": {
    "format_command": {
      "type": "string",
      "title": "Format command",
      "description": "Optional. Shell command to format code (e.g. 'npm run format'). Runs at Stop; if it exits non-zero the turn is blocked until fixed. Leave empty to disable."
    },
    "lint_command": {
      "type": "string",
      "title": "Lint command",
      "description": "Optional. Shell command to lint (e.g. 'npm run lint'). Runs at Stop; if it exits non-zero the turn is blocked until fixed. Leave empty to disable."
    },
    "test_command": {
      "type": "string",
      "title": "Test command",
      "description": "Optional. Shell command to run tests (e.g. 'npm test'). Runs at Stop; if it exits non-zero the turn is blocked until fixed. Leave empty to disable."
    },
    "auto_rollup": {
      "type": "string",
      "title": "Auto session rollup",
      "description": "Set to 'off' to disable the automatic end-of-session rollup note (files touched + edit count) written to memory. Enabled by default; a rollup is written only when the session made edits."
    },
    "context_budget_warn_pct": {
      "type": "string",
      "title": "Context budget warning %",
      "description": "Optional. Set to a percent (e.g. '75') to get a one-line context-usage nudge at the start of each turn once the context window is at least that full, prompting a /checkpoint + /compact. Leave empty to disable (default)."
    },
    "context_window_tokens": {
      "type": "string",
      "title": "Context window size (tokens)",
      "description": "Optional. Token size of your model's context window, used as the denominator for the budget percent. Defaults to 200000. Set higher for larger-context models."
    }
  }
}
